{
  "name": "geo_suite",
  "version": "3.0.0",
  "description": "ReEarth_GeoSuite: Re:Earth版 geo_suite を目指す統合プラグイン／ツール集",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p geo_suite/tsconfig.json && rm -rf dist && mkdir -p dist && cp -r geo_suite/build/* dist/ || true && cp -r index.html ryu.html dist/ || true && cp -r image dist/ || true"
  },
  "keywords": [
    "reearth",
    "plugin",
    "gis",
    "visualization"
  ],
  "author": "yamamoto-ryuzo",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
