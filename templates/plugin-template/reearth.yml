{
  "id": "{{PLUGIN_ID}}",
  "name": "{{PLUGIN_NAME}}",
  "version": "1.0.0",
  "description": "{{PLUGIN_DESCRIPTION}}",
  "author": "yamamoto-ryuzo",
  "schema": {
    "properties": {}
  },
  "extensions": [
    {
      "id": "main",
      "type": "widget",
      "name": "{{PLUGIN_NAME}} Widget",
      "description": "Main widget for {{PLUGIN_NAME}}",
      "widgetLayout": {
        "defaultLocation": {
          "zone": "outer",
          "section": "left",
          "area": "top"
        }
      }
    }
  ]
}
