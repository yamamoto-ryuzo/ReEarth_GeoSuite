import React from "react";

export default function App(): JSX.Element {
  return (
    <div style={{ padding: 24 }}>
      <h1>🌍 Re:Earth GeoSuite</h1>
      <p>このブランチは React 移行のベースです。既存の `geo_suite` ビルドは維持されています。</p>
      <p>続けてコンポーネント分割やルーティングを行ってください。</p>
    </div>
  );
}
